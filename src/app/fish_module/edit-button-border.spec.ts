import { EditButtonBorder } from './edit-button-border';

describe('EditButtonBorder', () => {
  it('should create an instance', () => {
    const directive = new EditButtonBorder();
    expect(directive).toBeTruthy();
  });
});
