import { FishCardBorder } from './fish-card-border';

describe('FishCardBorder', () => {
  it('should create an instance', () => {
    const directive = new FishCardBorder();
    expect(directive).toBeTruthy();
  });
});
